# this will accumulate code as I implement more codegen to llvm ir

1;

1 + 2;

2 * 125 + 256;

fn main(arg1: i64, arg2: i64): i64 {
    (arg1 + arg2 * 2 + arg1 + arg2 + arg2 * 2 + arg2) * 4 + 5;
};

fn ifn(arg: i64): i64 {
    if ( arg < 10 ) {
        -1;
    } else {
        arg * 2;
    };
}