#pragma once

#include "reg_alloc.h"

namespace reg_alloc {
    bool build_ifg(RegAlloc* alloc) {
        warn; return true;
    }
    bool coalesce(RegAlloc* alloc) {
        warn; return true;
    }
    bool color_ifg(RegAlloc* alloc) {
        warn; return true;
    }
};